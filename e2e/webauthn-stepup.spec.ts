import { test } from '@playwright/test';

test.describe('WebAuthn step-up', () => {
  test.skip('registers a passkey and enforces dashboard step-up', async () => {
    // Placeholder for CI environments with Firebase emulator + virtual authenticator wiring.
  });
});
